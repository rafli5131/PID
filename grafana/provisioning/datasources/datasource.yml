apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    database: air_quality_monitoring
    user: admin
    secureJsonData:
      password: adminpassword
    jsonData:
      sslmode: "disable"
    isDefault: true
